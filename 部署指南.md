# 部署指南

按顺序做完这 6 步，系统就会每天自动运行并推送到你的 iPhone。

---

## 第 1 步 · 推送到 GitHub

用 GitHub Desktop：

1. `File` → `Add local repository` → 选 `E:\投资探索\zhonggai-hongli`
2. 若提示「不是 Git 仓库」，点 `create a repository`
3. 左下角填提交信息（如 `init`），点 `Commit to main`
4. 点 `Publish repository`
5. **仓库名填 `zhonggai-hongli`**
6. **取消勾选 `Keep this code private`**
   —— GitHub Pages 免费版需要公开仓库

---

## 第 2 步 · 开启 GitHub Pages

仓库页面 → `Settings` → 左侧 `Pages`：

- Source 选 **Deploy from a branch**
- Branch 选 **main**，文件夹选 **/docs**
- 点 `Save`

约 1 分钟后页面地址是：

```
https://你的用户名.github.io/zhonggai-hongli/
```

**拿到地址后，把它填进 `config.yaml` 的 `page_url`**，这样 Bark 推送就能点击跳转。

---

## 第 3 步 · 添加 Secrets

仓库页面 → `Settings` → 左侧 `Secrets and variables` → `Actions` → `New repository secret`

依次添加两个（名字必须完全一致，全大写）：

| Name | Secret |
|---|---|
| `BARK_KEY` | 你的 Bark Key |
| `LIXINGER_TOKEN` | 理杏仁 token |

添加后只显示名字不显示内容，这是正常的。

> ⚠️ 这两个值不要写进任何代码文件，也不要发给任何人。

---

## 第 4 步 · 手动跑一次验证

仓库页面 → `Actions` → 左侧 `每日监测` → 右侧 `Run workflow`：

- `强制测试推送` 填 **true**
- 点绿色 `Run workflow`

约 1 分钟后：

- 手机应收到一条 Bark 推送
- Actions 日志末尾应显示提交了 `data/history.json` 等文件
- 打开 Pages 地址应能看到看板

**如果没收到推送**：检查 `BARK_KEY` 是否填错（只能删掉重加）。
**如果看板显示「读取 data.json 失败」**：等 Actions 跑完再刷新。

---

## 第 5 步 · 配置 cron-job.org 定时触发

> GitHub Actions 自带的 `schedule` 在仓库低活跃期会被自动暂停，所以用外部定时器。

1. 注册 [cron-job.org](https://cron-job.org)（免费）
2. `Create cronjob`，填：

| 字段 | 内容 |
|---|---|
| URL | `https://api.github.com/repos/你的用户名/zhonggai-hongli/actions/workflows/daily.yml/dispatches` |
| Request method | `POST` |
| Headers | `Authorization: Bearer 你的GitHub_PAT`<br>`Accept: application/vnd.github+json`<br>`Content-Type: application/json`<br>**`X-GitHub-Api-Version: 2022-11-28`** |
| Request body | `{"ref":"main"}` |
| Time zone | `Asia/Shanghai` |
| Schedule | 每天 **21:14** |

> **为什么必须写 `X-GitHub-Api-Version`**：不写的话 GitHub 用默认版本，
> 而「不指定版本」这种用法已于 2026-03-10 弃用、**2028-03-10 停止服务**。
> 到期后定时触发会静默失效 —— 而本策略周期跨到 2033 年，2028 年正在中途。
> 响应头里若出现 `Deprecation` / `Sunset` 两行，就说明这个头没加上。

另外把 **「Notify me when execution of the cronjob fails」打开** ——
这是 cron-job.org 自己挂掉时你唯一的早期预警。

GitHub PAT 生成：`GitHub 头像` → `Settings` → `Developer settings` →
`Personal access tokens` → **`Fine-grained tokens`** → `Generate new token`：

- `Repository access` → **Only select repositories** → 选 `zhonggai-hongli`
- `Permissions` → `Repository permissions` → **`Actions`** 设为 **`Read and write`**
- 其余全部 `No access`

> ⚠️ 这个 token 只存在 cron-job.org 里。截图分享前务必涂掉 Authorization 那一行。

> 为什么设成每天而不是工作日：看板有停摆检测，只在工作日跑的话周末会误报。
> 非交易日流水线会检测到并静默退出。

> 为什么不用 GitHub 自带的 `schedule`：一是仓库低活跃期会被自动暂停，
> 二是两个定时器时间接近会重复触发、每天推两条。`daily.yml` 里已删除内置 schedule，
> **触发权完全交给 cron-job.org**。

---

## 第 6 步（可选）· 开启跨设备确认同步

**不做这一步，确认按钮只在当前浏览器生效**（系统运行完全不受影响）。

1. `GitHub 头像` → `Settings` → `Developer settings` →
   `Personal access tokens` → **`Fine-grained tokens`** → `Generate new token`
2. 设置：
   - `Repository access` → **Only select repositories** → 选 `zhonggai-hongli`
   - `Permissions` → `Repository permissions` → **`Actions`** 设为 **`Read and write`**
   - **其他权限全部保持 `No access`**（尤其不要给 `Contents` 写权限）
3. 生成后复制 token
4. 编辑 `docs/app.js` 顶部三行：
   ```js
   var GH_OWNER = '你的用户名';
   var GH_REPO  = 'zhonggai-hongli';
   var GH_TOKEN = '刚才复制的token';
   ```
5. 提交推送

### ⚠️ 这一步的风险，请确认你理解

该 token 会**出现在公开网页的源码里，任何人都能看到**。

因为权限被限制为「仅本仓库 + 仅 Actions 写」，且 `confirm.yml` 对输入做了严格格式校验
（只接受 `ack:日期:动作:日期`，已通过路径穿越与命令注入测试），
**攻击者最多只能写入一条格式合法的确认记录，无法写入任意内容、无法读取 Secrets、
无法影响其他仓库、无法修改代码。**

这是在「跨设备同步」需求下能做到的最小暴露面。若你改变主意，
把 `GH_TOKEN` 改回空字符串即可，确认功能降级为纯本地。

---

## 日常使用

- **交易日 21:15 左右**收到 Bark 推送，第一行就是今天该做什么
- 点推送打开看板，执行后点 **「我已执行」**
- **没点确认也不影响系统**：引擎按规则继续推进持仓状态

### 你会收到哪几类推送

| 图标 | 含义 | 提醒级别 |
|---|---|---|
| 🔴 | 明日需操作 | 强提醒 |
| 🟠 | 今日已执行 | 强提醒 |
| 🟡 | **临近触发，请留意** | 强提醒 |
| ⚪ | 今日无操作 | 正常提醒 |
| 💚 | 系统正常 · 心跳 | 静默 |
| ⛔ | **需要你处理**（切换条款失明／份额折算／均线停摆／账本冲突） | 强提醒 |

**每个交易日必有一条**，最少也是 ⚪。只有非交易日才没有。

> 若嫌 ⚪ 太吵，把 `config.yaml` 的 `quiet_push_level` 改成 `passive` 即可 ——
> 那样它仍会送达，但不亮屏不响铃，只躺在通知中心里。

### 关于「开启止盈保护」

执行文档里这一步叫**武装**。看板和推送上不用这个词，因为它不直观。
它的实际含义是：

> 中概这份市值涨到 40 万（约 +33%）之后，系统才开始盯 MA250。
> 在那之前无论怎么跌都不会提示你卖 —— 因为还没赚到值得保护的浮盈。
> 之后一旦跌破 MA250，就会推 🔴 提示全部卖出锁定收益。

代码注释里仍保留「武装」字样，用于和执行文档对照。

### 关于临近触发提醒

观察点进入下面的区间后，当日推送标题会升级为 🟡，看板顶部也会出现虚线框的临近横幅：

| 类型 | 阈值 | 配置项 |
|---|---|---|
| 红利网格买卖、中概止盈清仓 | 还需变动 ≤ **1.5%** | `near_alert.price_pct` |
| 中概开启止盈保护 | 还差 ≤ **5%** | `near_alert.value_pct` |
| 切换条款 | 还差 ≤ **5 个百分点** | `near_alert.pb_pp` |

**临近 ≠ 要操作。** 横幅上写着「尚未触发，无需操作」，真到点的当天会另发 🔴 指令。
它的作用只是让你提前几天心里有数。

### 关于心跳

系统故障的表现形式是**没有推送**，而人对「没发生的事」是无感的 ——
长假里收不到推送和系统已死，在手机上长得一模一样。

所以只要**连续 6 天没出过声**（`heartbeat_days`），系统就主动推一条 💚 报平安。

**由此得到一条可靠的判断规则：**

> 超过一周完全收不到任何推送 = 系统出问题了，去看 Actions 和 cron-job.org。

心跳只在「本该静默」的日子发。正常交易周每天都有推送，心跳不会出现。

## 不需要你手工维护任何东西

系统自己知道它让你做过什么,并按 **T+1 收盘价**假定你照做了。
分红也是自动的 —— `dividends.py` 每周从天天基金抓除息日和每份派现,
再用腾讯复权价反推做交叉校验,两源不符会报警。

当前三个标的的分红情况:

| 代码 | 分红 |
|---|---|
| 513050 中概互联 | 从不分红 |
| 512890 红利低波 | 从不分红,收益留存在净值里 |
| 515180 中证红利 | 每年 1 次,10 月下旬,约每份 0.061 元 —— **自动入账** |

### ⚠️ `ledger.csv` 只记「和系统说的不一样」的部分

**照着做了就什么都不用记。** 引擎已经模拟了每一次定投、加码、网格成交,
你再记一遍等于同一笔买了两次。

实测把 26 次定投「如实」抄进账本的后果:

| | 不记账本 | 26 次全记 |
|---|---|---|
| 份额 | 269,350 | **336,669**(虚增 25%) |
| 现金 | 1,005 | **−76,020**(穿负) |
| 实际完成定投 | 26 次 | **13 次**(现金抽干后静默跳过) |

真出现这种情况,看板顶部会弹「账本冲突」红条,并指出该删哪一行。

**那什么时候才需要动它?** 只有三种:

| 情形 | 记什么 |
|---|---|
| 系统说买,你没买(或忘了) | `sell` 冲掉它假定的那笔 |
| 你买的份数/价格和收盘价差很多 | `buy`/`sell` 记差额部分 |
| 你自己额外买卖了 | 如实记 |

格式不变:

```csv
date,part,action,code,shares,price,amount,note
2026-09-02,1,sell,sh513050,5179,1.1140,5769.00,这周忘了下单，冲掉系统假定的定投
```

---

## 系统会出哪些错，以及它怎么告诉你

这些不是 bug，是一个跑 7-8 年的自动系统**注定会遇到**的事。
每一种都做了检测，出现时看板顶部会弹红/黄框，**写明发生了什么 + 你该做哪几步**，
Bark 也会同步推一条标题为「⛔ 需要你处理」的强提醒。

| 会发生什么 | 后果 | 系统怎么处理 |
|---|---|---|
| 定时触发或 Actions 停了 | 看板凝固在旧数据，且**它自己不会知道** | 浏览器端拿当前时间比对 `generated_at`，停 3 天即红框 |
| 理杏仁 token 过期／额度用尽 | **切换条款失明**——PB≤20% 全部清仓那条不再监控 | 断供 5 个交易日即红框，指明改哪个 Secret |
| ETF 份额折算 | 市值算错约一半，**40万止盈门槛永远够不到** | 红框 + 算出精确比例 + 给出可粘贴的 ledger 行。**唯一需要你动手的故障**，见下节 |
| 某个数据源改版 | 整行被跳过，均线窗口变旧 | 四源降级自动切换；连续跳过报黄框 |
| 四个源同时挂 | 策略无法推进 | 红框 + 强提醒 |
| MA250 断链 | 对应买卖点**静默失效 250 个交易日** | `ma_health` 检测，红框 |
| 账本记重了 | 持仓虚增、现金穿负、后续定投被静默跳过 | `check_duplicates` 指出该删哪一行 |
| 分红源改版 | 分红漏记，收益被低估 | 分红表 60 天未刷新报黄框 |
| 浏览器禁用本地存储 | 「我已执行」存不住 | 检测到即提示，系统运行不受影响 |
| 长假 / 非交易日 | 没有推送 | 心跳保证至少 6 天出一次声 |

### 份额折算：唯一需要你动手的故障

**这件事系统修不了，必须你改一行。** 几年才遇一次，但不处理后果是永久的。

折算时基金把净值按比例调低、份数按同比例调高，你的总市值不变。
但数据源给的是**不复权价**，系统只看到价格腰斩、份数没变 —— 于是：

| | 系统以为 | 你账户真实 | 偏差 |
|---|---|---|---|
| 份额 | 202,025 | 383,335 | −181,310 |
| 市值 | 199,806 | 300,795 | **−100,989** |
| 账面浮亏 | −47.2% | +0.1% | −47.4pp |

最要命的是：**开启止盈保护的 40 万门槛，系统看到的是 19.98 万，永远也到不了**
—— 不处理的话止盈保护会永久失效，而看板上不会有任何异常。

**系统会替你做的：**

- 检出（单日跳变 >15%，A股ETF涨跌停只有10%，这个幅度只可能是折算）
- **精准冻结**：停掉止盈与加码（要拿现价比历史基准，基准已污染），
  定投照常（日历规则，按当天真实价买固定金额，不受影响）
- **把折算比例算给你**：用后复权/不复权比值反推，是精确值。
  512890 那次真实折算实测得 `2.000000`；用收盘价直除得 `2.046192`，
  差 2.31%，因为那把当天真实的 −2.26% 跌幅混了进去
- 在看板顶部弹红框，把下面每一步连同**可以直接粘贴的那行**都写出来

**你要做的三步：**

1. 到基金公司公告页核对比例（红框里已给出系统推算值）
2. 在 `ledger.csv` 末尾加一行，**比例填在 shares 列**：
   ```csv
   2027-03-15,1,split,sh513050,2,0,0,份额折算
   ```
3. 把日期写进 `config.yaml` 的 `resolved_splits`：`resolved_splits: ["2027-03-15"]`

提交推送、跑一次 Actions 即可。实测修完后市值与「没发生折算的世界」**偏差 0 元**。

**修完之后止盈还会继续冻结约 250 个交易日**，红框会降级成黄框并显示倒计时。
这不是没修好 —— MA250 要 250 根K线，窗口里还跨着折算那天的断点，
算出来的均线没有意义。这段时间不需要你做任何事，到期自动恢复；
期间若想自己判断，直接看行情软件里的 MA250（它是复权的，没有这个问题）。

---

## 出问题时

| 现象 | 处理 |
|---|---|
| 收到「数据异常」推送 | 看 Actions 日志，通常是某个数据源改版；四源降级会自动切换 |
| 看板数字不动 | 检查 Actions 是否在跑；cron-job.org 是否还在触发 |
| **超过一周没收到任何推送** | 心跳也停了 = 系统层面出问题。依次查 cron-job.org 的执行历史 → Actions 运行记录 → Bark Key |
| 想重建状态 | 改完 `ledger.csv` 后运行 `python src/init_state.py` |
| 分红没入账 | 运行 `python src/dividends.py` 看两源是否都挂了 |
| 看板报「账本冲突」 | 按提示删掉 `ledger.csv` 里重复的那行 |
| 怀疑规则跑偏 | 运行 `python src/backtest.py`，114 项验收全过才算正常 |
| 看板报「发生份额折算」 | 按红框里的三步做，最关键是那行可粘贴的 `split` 记录 |
